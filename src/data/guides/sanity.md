접속: https://sanity.io

## 조회해야 할 값
Project ID
Dataset name (기본값: production)

- 위 두 값을 찾은 경우 이 페이지에서 할 일을 마칩니다.
- 구체적인 반환 형식
{ "steps": [] }

## 조회 경로
1. Start Building
2. Create (or select) a project → Project ID
3. API tab
4. Add (or selelct) an API tokens
5. Datasets tab
6. Add (or select) a dataset → Dataset name


## 복사해야 할 환경변수

Project ID → VITE_SANITY_PROJECT_ID (google-ai-studio)
Dataset name (기본값: production) → VITE_SANITY_DATASET (google-ai-studio)
